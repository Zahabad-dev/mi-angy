{
  "name": "nextjs-node-app",
  "version": "1.0.0",
  "description": "Next.js + Node.js application for Vercel",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix api\"",
    "build": "npm run build --prefix frontend && npm run build --prefix api",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": "24.x"
  }
}
